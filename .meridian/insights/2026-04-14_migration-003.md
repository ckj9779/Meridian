<!-- Copyright (c) 2026 ckj9779. Licensed under BSL 1.1. See LICENSE. -->

# Insight Ledger — Migration 003: Insight Reconciliation

- **Date:** 2026-04-14
- **Prompt:** Migration 003 — schema/seed alignment, DATABASE.md updates
- **Session:** 07
- **Prior commit:** `8c56bbd`
- **Resolves:** INS-004 through INS-011 from `2026-04-14_phase0-scaffold.md`

## Insights

### INS-002 | failure_and_fix | Step 7
- **Finding:** Step 7 smoke test initially appeared to pass but was actually hitting a zombie `tsx src/index.ts` process (PID 77212) left over from Session 07 Step 6's background smoke test — it never got a clean SIGTERM and stayed bound to `0.0.0.0:3000`. My new server process failed to start (port in use, "failed with exit code 1") but curl still returned 200 from the zombie. Killed via `Stop-Process -Force` after identifying the listener via `netstat`. Re-ran smoke test cleanly: new process bound, `/health` returned `200 database:connected`.
- **Routes to:** None (operational hygiene, not a doc bug).
- **Action:** In future sessions, kill dev servers explicitly with TaskStop before ending a session. Consider documenting a port-3000-check pattern in GITOPS.md or a scripts/kill-dev.sh helper.
- **Severity:** warning

### INS-001 | convention_gap | Step 3
- **Finding:** During the structural seed-JSON rewrite, 4 `legacy_issues` entries (CAG-01..CAG-04) were found to have `severity: null` rather than a string value. Prior-session INS-008 only flagged the `"normal"` → `"medium"` mapping; null was never observed because the legacy issues weren't successfully reaching the script's mapping path. The seed script's `normalizeSeverity()` already defaults non-strings to `medium`, so reseed will succeed, but the JSON has factually-missing severity for all CAG-* legacy issues.
- **Routes to:** `.meridian/data/seed/meridian-seed-data.json` (`legacy_issues` array).
- **Action:** Owner decision — assign severity values to the 4 CAG legacies, or accept `medium` as the implicit default.
- **Severity:** info

---

## Routing Summary

Pure application session — three prior-session routing items resolved in bulk. Only two net-new insights, both low-severity.

| # | Insight | Target | Action |
|---|---------|--------|--------|
| INS-001 | `.meridian/data/seed/meridian-seed-data.json` | Assign explicit `severity` to the 4 CAG legacy issues, or document `medium` as the default. |
| INS-002 | Operational hygiene | Ensure dev servers are killed (TaskStop) before ending a session. Consider a `scripts/kill-dev.sh` helper or a port-3000-check in a pre-session guard. |

### Scaffold-session insights resolved by this commit

| Scaffold INS | Status | Resolution mechanism |
|---|---|---|
| INS-004 | ✅ resolved | DATABASE.md Seed Data section rewritten to describe JSON + `seed.ts` + `--reseed` (Step 6c). |
| INS-005 | ✅ resolved | DATABASE.md overview updated PG 16 → PG 18 with AGE-PG18 Phase 2 blocker callout (Step 6a). |
| INS-006 | ✅ resolved | DATABASE.md connection section documents `rejectUnauthorized: false` Railway workaround (Step 6b). |
| INS-007 | ✅ resolved | Seed JSON renamed `session` → `session_number`; migration 003 added `decided_at` + `related_issues` columns; seed backfilled. |
| INS-008 | ✅ resolved | Seed JSON `"normal"` rewritten to `"medium"` (structural); 4 CAG legacies surfaced new sub-finding (INS-001 above). |
| INS-009 | ✅ resolved | Seed JSON renamed `title`/`notes` → `summary`/`resolution`; migration 003 added `description`, `phase`, `related_decisions` columns; seed backfilled. |
| INS-010 | ✅ resolved | Seed JSON renamed `number` → `session_number`, `key_deliverables` → `artifacts_produced` (wrapped as text[]); migration 003 added `summary` column; seed backfilled. |
| INS-011 | ✅ resolved | Seed JSON renamed `task`/`model` → `task_type`/`model_id`; `is_default: true` added to all 4 entries. |

### Scaffold-session insights still open

| Scaffold INS | Reason still open |
|---|---|
| INS-001 | Resolved last session (seed path `.meridian/data/seed/`). DATABASE.md now documents this correctly. |
| INS-002 | Info-only (prompt-vs-doc tsconfig discrepancy). CODING_STANDARDS already correct; no fix required. |
| INS-003 | Info-only (prompt should have listed zod). Already listed as prod dep. No fix required. |
| INS-014 | Resolved (`.gitignore` fixed in scaffold commit `799ae70`). |

Net state: **all actionable scaffold-session gaps are now resolved or explicitly deferred.**

## Session Statistics

| Metric | Value |
|--------|-------|
| Total new insights captured | 2 |
| Convention gaps | 1 (INS-001) |
| Failures and fixes | 1 (INS-002 — zombie server) |
| Scaffold insights resolved | 8 (INS-004 through INS-011) |
| Scaffold insights reviewed and closed as info-only | 4 (INS-001, INS-002, INS-003, INS-014) |
| Migrations applied | `003_schema_seed_alignment` (3 tables altered, 6 columns added) |
| Seed JSON renames | 8 field-rename operations across 6 top-level arrays; 4 severity `normal` → `medium` values; 4 `is_default: true` additions |
| Seed mode | `--reseed` (truncate + reload) — 32 decisions, 23 issues, 6 sessions, 4 model_preferences |
| DATABASE.md edits | 6 surgical edits (`:9` PG version, `:42-44` SSL note, `:98` table count, `:365-366` decisions cols, `:388-390` issues cols, `:347` sessions col, `:400-418` stack_components, `:518-522` seed section) |
| Files created | 2 (`migrations/003_schema_seed_alignment.sql`, this ledger) |
| Files modified | 5 (`src/scripts/seed.ts`, `.meridian/data/seed/meridian-seed-data.json`, `docs/DATABASE.md`, `.meridian/insights/2026-04-14_migration-002.md` trailing routing summary, `.claude/settings.local.json`) |
| Full DB state post-reseed | 32 decisions, 23 issues, 6 sessions, 1 system_context, 4 model_preferences, 8 stack_components, 0 tech_watch, 0 sources, 3 schema_migrations |
| Health check | `GET /health` → 200, `database: connected` (after killing zombie PID 77212) |
| Commit hash | `d040b59aa459e80260998a75f96d40258fbc6963` (short: `d040b59`) |
| Signature verified locally | Yes — `Good signature from "Charles K. Johnson <mobile@charleskjohnson.com>"` |
| Pushed to GitHub | No (owner pushes manually per prompt) |

