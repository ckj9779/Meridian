<!-- Copyright (c) 2026 ckj9779. Licensed under BSL 1.1. See LICENSE. -->

# Insight Ledger — Migration 002: Schema Reconciliation

- **Date:** 2026-04-14
- **Prompt:** Migration 002 — system_context seed, stack_components table
- **Session:** 07
- **Prior commit:** `799ae70`
- **Resolves:** INS-012 (system_context shape), INS-013 (tech_watch concept split)
- **Decisions:** D33 (system_context stays versioned doc), D34 (split tech inventory from event log)

## Insights

### INS-001 | convention_gap | Step 2
- **Finding:** `stack_components` DDL uses `serial` primary key as specified in the prompt, but `docs/DATABASE.md:53` establishes the project convention: "Primary keys: uuid, generated with `gen_random_uuid()`." Every other table in Phase 0 follows uuid (`decisions` and `issues` use human-readable varchars as explicit exceptions). `stack_components` is the first table in the schema to use a serial integer PK.
- **Routes to:** `docs/DATABASE.md:53` — convention statement; `migrations/002_stack_components.sql`.
- **Action:** Owner decision — either (a) change `stack_components.id` to `uuid PRIMARY KEY DEFAULT gen_random_uuid()` for consistency (requires migration 003 since 002 is applied), or (b) update DATABASE.md to note serial PKs as acceptable for small static inventory tables. Followed prompt as written for now.
- **Severity:** warning

### INS-002 | convention_gap | Step 6
- **Finding:** The 8 stack_components rows came from the prompt's inline table, **not** from `meridian-seed-data.json`. The seed JSON's `tech_watch` array still contains the old inventory-shaped data (component/version/status/phase/notes), which is now redundant with `stack_components` but still on disk.
- **Routes to:** `.meridian/data/seed/meridian-seed-data.json`.
- **Action:** Owner decision — either (a) remove the `tech_watch` array from seed JSON (it maps to `stack_components` now) and optionally add a `stack_components` array mirroring the prompt's data so the seed source is a single JSON file, or (b) leave JSON alone and accept that this session's stack data lives in `src/scripts/seed-002.ts`. Current split means two sources of truth for technology inventory.
- **Severity:** info

### INS-003 | recommendation | Step 8
- **Finding:** gpg-agent passphrase cache expired a second time during this session (roughly every 10 minutes of elapsed wall-clock between commits). `docs/GITOPS.md` (after last session's INS-009 routing work) recommends `default-cache-ttl 7200` in `~/.gnupg/gpg-agent.conf`, but it has not been applied to the WSL environment yet.
- **Routes to:** User's `/home/cjohnson/.gnupg/gpg-agent.conf`.
- **Action:** Apply the recommended config once — eliminates repeated passphrase warmups during long multi-step sessions. Not a doc change; just operational hygiene.
- **Severity:** info

### INS-004 | convention_gap | Step 2
- **Finding:** `stack_components` is not yet documented in `docs/DATABASE.md`'s "Relational Schema — 15 Tables" section. The section header says "15 tables" but now there are 16 (plus the `schema_migrations` tracker, which isn't counted in the 15).
- **Routes to:** `docs/DATABASE.md` — section header count and new table DDL documentation.
- **Action:** Add `stack_components` to the relational schema listing, update "15 Tables" → "16 Tables" in the section header.
- **Severity:** info

---

## Routing Summary

Pure application session — design locked before execution (D33, D34). Four
insights captured, all info/warning severity, none blocking. Migration 002
resolves the two critical gaps from Session 07's scaffold ledger
(INS-012 and INS-013) cleanly.

| # | Insight | Target Doc | Section | Action |
|---|---------|-----------|---------|--------|
| INS-001 | `docs/DATABASE.md` + possibly migration 003 | Schema Conventions (`:48-58`) | Decide: align `stack_components.id` to uuid OR document serial-PK exception. |
| INS-002 | `.meridian/data/seed/meridian-seed-data.json` | `tech_watch` array | Decide: drop `tech_watch` array (now obsolete) or add `stack_components` array to mirror the new data. Single source of truth for stack data. |
| INS-003 | User's `~/.gnupg/gpg-agent.conf` | (ops, not docs) | Apply `default-cache-ttl 7200` + `max-cache-ttl 14400`. One-time setup per GITOPS.md. |
| INS-004 | `docs/DATABASE.md` | Header (`:94`) + table listing | Add `stack_components` DDL reference; bump "15 Tables" to "16 Tables". |

## Session Statistics

| Metric | Value |
|--------|-------|
| Total insights captured | 4 |
| Environment discoveries | 0 |
| Assumptions validated | 0 |
| Assumptions invalidated | 0 |
| Convention gaps | 3 (INS-001, INS-002, INS-004) |
| Failures and fixes | 0 |
| Recommendations | 1 (INS-003) |
| Security observations | 0 |
| Dependencies flagged | 0 |
| Critical gaps | 0 |
| Prior-session critical gaps resolved | **2** (INS-012 and INS-013 from `2026-04-14_phase0-scaffold.md`) |
| Files created | 3 (`migrations/002_stack_components.sql`, `src/scripts/seed-002.ts`, this ledger) |
| Files modified | 2 (`.meridian/insights/2026-04-14_phase0-scaffold.md` routing summary tail, `.claude/settings.local.json`) |
| Migration applied | `002_stack_components` (1 table, 2 indexes) |
| Tables populated | `system_context=1` (v1 active), `stack_components=8` |
| Full DB state | 32 decisions, 23 issues, 6 sessions, 1 system_context, 4 model_preferences, 8 stack_components, 0 tech_watch, 0 sources, 2 schema_migrations |
| Commit hash | `8c56bbd2e6d4ff4ca72887e03db67ec99bad1240` (short: `8c56bbd`) |
| Signature verified locally | Yes — `Good signature from "Charles K. Johnson <mobile@charleskjohnson.com>"` |
| Pushed to GitHub | No (owner pushes manually per prompt) |
